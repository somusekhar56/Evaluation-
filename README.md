# GIT
# 1. What is git
Git is a version control system(VCS)-A rool that helps develpores to track changes in their code.
# 2 What is difference between git and github
# Git:                  
Git is version control system.
works locally
insatlled on your system
# Github
Git hub is could based platform
works online
It stores your Git repositories online.
# 3 Use of status command
Status command is used to see what are the files are stagged and unstagged in the local.
# 4 What is the command to initialize the git
We use git init command to initialize the git.
# 5 What is the use of push command
By using the push command we can able to upload the files and changes we made in the local to the remote repository.
# 6 Difference between fetch and pull
Git fetch only downloads the files from remote to the local and it doesn't merge the files automatically.
Git pull download and merge the files from remote to the local automatically. Gitpull=Gitfetch + Gitmerge.
# 7 What is the use of git ignore
By using .gitignore we can able to ignore the unwanted files like cache.It will tell what are the files have to be untrack.
# 8 What is the command for know the commit history
git log is the command we can use for see the commit history.
# 9 Command for switching the branch
Git checkout is the command used for to switching the branch.
# 10 What is merge conflict and how to fix it
when two or more users can edit the same file and they want to push that file the merge conflicts will occur.
Then the admin will have the notification in remote as merge conflict will occur then the admin have to decide which changes will be there then solve the conflicts and merge the changes into the main repository.

# PYTHON
# 1 Use of python
Python is the high level interpreter language, scalable, faster and secure.
# 2 What is set
A set is a collection of unique items.It does not allow duplicates and does not maintain order.
# 3 What is for loop
A for loop is a programming construct used to repeat a block of code a specific number of times.used to iterate over a sequences,such as a list, string,or merge of numbers.
# Syntax
for i in range(5):
    print(i)
# 4 What is slicing
Slicing = taking a part of a sequence using start, end, and step values.
 # Slicing Syntax
sequence[start : end : step]
# 5 We have list print the line by line.
<img width="877" height="182" alt="image" src="https://github.com/user-attachments/assets/51e3b2cd-6a75-4773-9dd6-026d467f1976" />

# 6 we have a name with string print the last sekhar
<img width="1200" height="285" alt="image" src="https://github.com/user-attachments/assets/db91b059-f08b-4731-99b1-f4b3c2d04c30" />

# 7 We have a list find even number from list
<img width="920" height="375" alt="image" src="https://github.com/user-attachments/assets/953de0b5-d296-4f56-ab25-13f1eb671a66" />

# SQL
# 1 what is sql
SQL (Structured Query Language) is a language used to store, manage, and retrieve data from a database.
# 2 what is database
A database is an organized collection of data that allows you to store, manage, and retrieve information easily.
# What is sub query
A subquery is a query inside another query.It is also called a nested query or inner query.
# 3 What is difference between delete,Truncate, drop
DELETE → Remove specific rows
TRUNCATE → Remove all rows (empty table)
DROP → Remove entire table (delete structure)
# 4 How to find the table
select * from employees
# 5 Find the total salary who have salary more than 50000
<img width="991" height="440" alt="image" src="https://github.com/user-attachments/assets/b83d5b92-6e8b-431f-b825-8ff8e23cb6d2" />

# 6 find the IT employees from the table

<img width="990" height="463" alt="image" src="https://github.com/user-attachments/assets/f0646963-72af-49bb-a66f-ac7a26a49d99" />












